<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medicalrecord">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MedCardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DetailDoctorActivity"
            android:parentActivityName=".activity.DoctorActivity"/>
        <activity android:name=".activity.DetailDiseaseActivity"
            android:parentActivityName=".activity.DiseaseActivity"/>
        <activity
            android:name=".activity.AddDrugActivity"
            android:parentActivityName=".activity.DrugActivity" />
        <activity
            android:name=".activity.AddDoctorActivity"
            android:parentActivityName=".activity.DoctorActivity" />
        <activity
            android:name=".activity.AddDiseaseActivity"
            android:parentActivityName=".activity.DiseaseActivity" />
        <activity android:name=".activity.DrugActivity" />
        <activity android:name=".activity.DoctorActivity" />
        <activity android:name=".activity.DiseaseActivity"/>
    </application>

</manifest>